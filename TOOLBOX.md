# Packets

Compute and communicate.

One set of notes per node. One seed per node.

The seed is the shared key:
how to read a packet, and how to optionally route one.

Both nodes agree what it is.
Both keep a copy.
Do not send until both agree.

The packet names a path. The file stays on disk.

Do not copy passwords or someone else’s home.
