# lan-probe

List local paths. Run before git.

```
../toolbox/lan-probe
```

Prints: hostname, OS, mounts (`/Volumes` or `/media`), addresses, ARP/neighbor table.

Use that list for [[send-file]] destinations.
