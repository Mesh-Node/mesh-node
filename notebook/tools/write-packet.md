# write-packet

Write a packet file.

```
../toolbox/write-packet OUTFILE who what files
```

Example:

```
../toolbox/write-packet inbox/this-host.md Mesh-Node/mesh-node "hello from this host" none
```

Fields: who, what, files. Files stay on disk. The note is not the files.

See [[inbox]].
