# procedure

The loop for a basic [[smart-node]]. Lives in `../toolbox/PROCEDURE.md`.

Order: create toolbox → [[lan-probe]] → local send/recv → same machine → LAN HTTP → harness doors already present → git last → stop.

Tick on disk: `procedure-state.md`. One line per step. Never wait.

GitHub is store-and-forward. Not the first path. The first path is local.
